{% extends "base.html" %}

{% block content_body %}
<div class=metanav>
  {% if user.is_authenticated() %}
    Create an event
  {% else %}
    <a href="{{ url_for('security.register') }}">Register</a>
  {% endif %}
  </div>
{% endblock content_body %}
